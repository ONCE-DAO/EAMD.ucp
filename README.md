# EAMD.ucp
```
git clone https://github.com/ONCE-DAO/EAMD.ucp.git
cd EAMD.ucp
git checkout build
npm i
npm run build:clean:ignoreErrors  // Temporär
npm run build                     // Temporär
npm start
```
